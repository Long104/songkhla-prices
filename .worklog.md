# Project Work Log

## 2026-07-19

### 15:00 — Added context and prototype for Socrates AI

- Created `hackaton/buildweekai/context.md` detailing the OpenAI Build Week rules.
- Created `hackaton/buildweekai/prototype.html` as an interactive, Socratic Middle Way prototype. Expanded the canvas to a 5-node diamond graph with three assumptions (2a, 2b, 2c). Restructured the node cards into Socratic Euthydemus-style split cards, separating the validated "Fact (True)" from the examined "Leap (Assumption)." Updated the chat logic to validate facts and target only the leaps, providing a deep 7-turn Socratic resistance simulation. Added an "Auto-Fill Demo" button to run the flow.
- Updated vault `index.md` and `log.md`.
- Staged, committed, and pushed all modifications to Git.

## 2026-08-10 — songkhla-prices worktree: i18n scaffolding

- `src/db/schema.ts` — removed `.nullsNotDistinct()` from `prices_product_source_province_date_idx` uniqueIndex; changed `prices_product_id_idx` uniqueIndex → plain `index`; added `index` to drizzle-orm/pg-core import.
- Created `src/i18n/routing.ts`, `src/i18n/request.ts`, `src/messages/th.json`, `src/messages/en.json`, `src/middleware.ts`.
- `next.config.ts` — wrapped with `createNextIntlPlugin('./src/i18n/request.ts')`, added `outputFileTracingRoot: __dirname`.
- `src/app/layout.tsx` — replaced Geist fonts with Inter, `suppressHydrationWarning`, Thai metadata.
- `src/app/page.tsx` — redirects to `/{defaultLocale}`.
- Created `src/app/[locale]/layout.tsx` — NextIntlClientProvider + Header/Footer shell (components pending).
- Build: `npx next build` compiles ✓; fails only on missing `@/components/header` / `@/components/footer` (expected). ESLint on changed files ✓.

## 2026-08-10 — songkhla-prices worktree: scraping system + cron endpoint

- `src/lib/scrapers/types.ts` — `ScrapedPrice`/`Scraper` interfaces, `fetchHtml` (10s AbortController timeout), `parsePrice`.
- `src/lib/scrapers/{dit,oae,taladthai,simummuang,eppo}.ts` — 5 scrapers implementing the `Scraper` interface (table-row extraction, error-safe).
- `src/lib/scrapers/index.ts` — exports `scrapers` array (dit, oae, taladthai, simummuang, eppo).
- `src/app/api/cron/scrape/route.ts` — POST endpoint: Bearer `CRON_SECRET` auth, runs all scrapers via `Promise.allSettled`, maps scraped names to products via `productSourceMappings`, upserts into `prices` with `onConflictDoNothing`, returns per-source status + `totalInserted` + `unmapped`.
- `vercel.json` — cron `0 22 * * *` → `/api/cron/scrape`.

### Build fix (typecheck)

- `src/app/api/cron/scrape/route.ts` — `prices.sourceDate` is drizzle `date()` (string mode), so `sp.sourceDate` (a `Date`) failed typecheck. Added local `toDateOnly()` helper converting to local `YYYY-MM-DD`. Build ✓, lint ✓.

### Seed script

- `src/db/seed.ts` — new standalone seed script: imports `getDb` from `@/db`, schema tables (`sources`, `categories`, `products`, `provinces`, `productSourceMappings`), `provincesSeed` from `@/lib/provinces`. Inserts 5 sources, 8 categories, 77 provinces, 35 products (category IDs resolved by slug via single select + Map). All inserts `onConflictDoNothing()`, null-DB guard, try/catch, progress logs, "Seed completed successfully". Product counts: meat 6 / vegetables 7 / rice 3 / eggs 3 / oil 2 / seasoning 5 / fuel 5 / fruit 4 = 35.
- `package.json` — added `"seed": "tsx src/db/seed.ts"` script.
- Smoke test: `npx tsx src/db/seed.ts` without DATABASE_URL prints "Database not available" + exit 1 ✓. `npx next build` exit 0 ✓ (only warning: intentional `productSourceMappings` import per spec, unused in this version).

## 2026-08-10 — songkhla-prices worktree: review fixes (province cookie → server queries + cron logging)

- `src/components/province-selector.tsx` — `handleChange` now also sets `province` cookie (`path=/; max-age=31536000`) alongside localStorage + `province-change` event so server components can read the selection.
- Created `src/db/queries.ts` — shared query helpers so category/search/product pages don't duplicate logic: `getProvinceIdByCode(db, code)` (DOPA code → DB id, null-safe), `provincePriceFilter(provinceId)` (province OR national, national-only fallback when province unknown), `getProductsWithCheapestPrice(db, rows, provinceId)` (per-product cheapest price + unit + distinct source count; per-product try/catch, never throws), `ProductWithCheapestPrice` type.
- `src/app/[locale]/category/[slug]/page.tsx` — reads `province` cookie (`await cookies()`, default `DEFAULT_PROVINCE_CODE`), select now includes `id`, product list enriched with cheapestPrice/cheapestUnit/sourceCount via helper, passed to `ProductCard` instead of hardcoded nulls.
- `src/app/[locale]/search/page.tsx` — same cookie read + `id` in select + cheapest-price enrichment for results.
- `src/app/[locale]/product/[slug]/page.tsx` — reads province cookie, resolves province id, price query filtered with `and(eq(prices.productId, ...), provincePriceFilter(provinceId))` (province OR national).
- `src/app/api/cron/scrape/route.ts` — empty `catch {}` on per-price upsert now logs `[cron] Failed to upsert price for "<name>":` + error.
- Verify: `npx next build` exit 0 ✓ (26/26 pages; category/search/product routes not statically prerendered — no HTML emitted, request-time render so cookies() works). Production smoke test `next start` + curl: `/th/category/meat` 200 with `province=10` cookie, `/th/search?q=หมู` 200, `/th/product/pork-belly` 404 (existing no-DB notFound), `/th` 200 ✓. `npx eslint src/` 0 errors (1 pre-existing warning in seed.ts, intentional per spec) ✓. No test files exist yet — nothing to run.

### [2026-08-10] [Pipeline] feat/songkhla-prices: grocery price comparison MVP

- **Changes**: Full Next.js 15 grocery price comparison app. 6-table Drizzle schema (sources, categories, products, provinces, prices, productSourceMappings). 5 government/wholesale scrapers (DIT, OAE, Talad Thai, Si Mum Muang, EPPO). Cron endpoint at /api/cron/scrape (05:00 ICT, CRON_SECRET auth). Pages: home (8 category cards), category listing (cheapest price per product), product detail (full price comparison table with cheapest highlight + unit mismatch warning), search (ILIKE Thai/English). Province selector with cookie persistence (default: Songkhla). Thai/English i18n via next-intl. Seed script: 35 products, 77 provinces, 8 categories, 5 sources. Shared query helpers in src/db/queries.ts.
- **Verification**: `npx next build` exit 0 (26/26 pages). `npx eslint src/` 0 errors. Smoke tests: /th 200, /th/category/meat 200, /th/search?q=หมู 200, POST /api/cron/scrape 401 without auth. Reviewer issues (province in server queries, price data on list pages, cron logging) fixed.
- **Outcome**: Phase 1 MVP complete on feat/songkhla-prices branch. Ready for Neon DB provisioning + Vercel deploy. Next: set DATABASE_URL, run seed, verify scraper cron against live gov sites.
